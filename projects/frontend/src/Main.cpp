#include "windowsManager.h"
#include <iostream>
#include <string>

int main(int, char**) {
    lib::DoorsWindowManager DOORS;
    std::cout << "all done";
    std::flush(std::cout);
    system("pause");

    return 0;
}
